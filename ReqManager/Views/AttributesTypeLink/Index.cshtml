@model IEnumerable<ReqManager.Entities.Link.AttributesTypeLinkEntity>

@{
    ViewBag.Title = "Type Link Attributes";
}


<table class="table">
    <tr>
        <th>
            Attribute
        </th>
        <th>
            Link Type
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Attributes.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeLink.description)
        </td>
        <td>
            <div class="dropdown">
                <a href="#" title="Options" class="btn btn-default" data-toggle="dropdown" aria-expanded="false">
                    <i class="fa fa-chevron-down icon_8"></i>
                    <div class="ripple-wrapper"></div>
                </a>
                <ul class="dropdown-menu dropdown-menu-right">
                    <li>@Html.ActionLink("Edit", "Edit", new { id = item.AttributesTypeLinkID })</li>
                    <li>@Html.ActionLink("Details", "Details", new { id = item.AttributesTypeLinkID })</li>
                    <li>@Html.ActionLink("Delete", "Delete", new { id = item.AttributesTypeLinkID })</li>
                </ul>
            </div>
        </td>
    </tr>
}

</table>
