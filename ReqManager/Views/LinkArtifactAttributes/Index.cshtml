@model IEnumerable<ReqManager.Entities.Link.LinkArtifactAttributesEntity>

@{
    ViewBag.Title = "Listing of Attributes for Link between Art. and Req.";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Attributes.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LinkRequirementsArtifacts.code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Attributes.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LinkRequirementsArtifacts.code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtefactAttributeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtefactAttributeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtefactAttributeID })
        </td>
    </tr>
}

</table>
