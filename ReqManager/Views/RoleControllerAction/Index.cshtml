@model IEnumerable<ReqManager.Entities.Acess.RoleControllerActionEntity>

@{
    ViewBag.Title = "Relationship between Roles and Actions";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model. ControllerAction.DisplayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ControllerAction.DisplayName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleControllerActionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleControllerActionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleControllerActionID })
        </td>
    </tr>
}

</table>
