@model IEnumerable<ReqManager.Model.RoleControllerAction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ControllerAction.controller)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ControllerAction.action)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ControllerAction.controller)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ControllerAction.action)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleControllerActionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoleControllerActionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleControllerActionID })
        </td>
    </tr>
}

</table>
