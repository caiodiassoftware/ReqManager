@model IEnumerable<ReqManager.Models.RoleControllerAction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_controllerAction)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_controllerAction)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_roleControllerAction }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_roleControllerAction }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_roleControllerAction })
        </td>
    </tr>
}

</table>
