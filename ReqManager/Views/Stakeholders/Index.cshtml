@model IEnumerable<ReqManager.Entities.Project.StakeholdersEntity>

@{
    ViewBag.Title = "Stakeholders";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Users.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StakeHolderClassification.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Users.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StakeHolderClassification.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StakeholderID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StakeholderID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StakeholderID })
        </td>
    </tr>
}

</table>
