@model IEnumerable<ReqManager.Models.UserRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_user)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_role)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_user)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_role)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_userRole }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_userRole }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_userRole })
        </td>
    </tr>
}

</table>
